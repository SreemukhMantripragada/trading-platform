# configs/risk_budget.yaml
total_cash_inr: 100000             # starting capital reference; runner will also query broker cash
holdback_fraction: 0.20            # always keep 20% un-deployed

buckets:
  LOW:
    split: 1.00
    max_per_trade: 25000
  MED:
    split: 0.00
    max_per_trade: 20000
  HIGH:
    split: 0.00
    max_per_trade: 10000
